{
  "name": "angular-youtube",
  "version": "1.0.0",
  "description": "Proyecto angularJS de videos de youtube",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json"
  },
  "author": "Jon",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.0.0",
    "json-server": "^0.15.1"
  }
}
